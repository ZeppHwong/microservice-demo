<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.demo.order.infra.mapper.OrderMapper">

    <!--    1、useGeneratedKeys="true" 表示给主键设置自增长。
        2、keyProperty="sid" 表示将自增长后的 Id 赋值给实体类中的 sid 字段。-->

    <insert id="placeOrder" parameterType="cn.demo.order.infra.valueobject.Order"
            useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        insert into order_tbl (user_id,commodity_code,count,money)
            values (#{userId},#{commodityCode},#{count},#{money})
    </insert>

</mapper>