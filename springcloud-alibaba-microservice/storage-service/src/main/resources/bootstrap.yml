spring:
  application:
    name: ali-storage-service
  cloud:
    nacos:
      discovery:
        server-addr: ${NACOSIP:127.0.0.1}:8848
#        username: nacos
#        password: nacos
      config:
        file-extension: yaml
        server-addr: ${NACOSIP:127.0.0.1}:8848
#        username: nacos
#        password: nacos
#    alibaba:
#      seata:
#        # Seata 事务组名称，对应file.conf文件中 vgroup_mapping.sca-customer-seata-tx-service-group
#        default_tx_group: my_test_tx_group
seata:
  enabled: true
  tx-service-group: my_test_tx_group