server:
  port: 8090
  servlet:
    context-path: /ali-account
spring:
  application:
    name: ali-account-service
  cloud:
    nacos:
      discovery:
        server-addr: ${NACOS:127.0.0.1}:8848
        username: nacos
        password: nacos
      config:
        file-extension: yaml
        server-addr: ${NACOS:127.0.0.1}:8848
        username: nacos
        password: nacos
